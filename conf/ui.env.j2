# Generated by gittr_ynh — do not edit by hand, see ynh_add_config calls in
# scripts/install, scripts/upgrade, scripts/change_url.
#
# NEXT_PUBLIC_* vars are inlined into the client JS bundle at `next build`
# time, not read at runtime — this file must exist (and the UI must be
# rebuilt) BEFORE `yarn build` runs, and again after any change_url.
NEXT_PUBLIC_DOMAIN=__DOMAIN__
NEXT_PUBLIC_SITE_URL=https://__DOMAIN__
NEXT_PUBLIC_GIT_SSH_BASE=__DOMAIN__
NEXT_PUBLIC_GIT_SERVER_URL=https://__DOMAIN__
NEXT_PUBLIC_NOSTR_RELAYS=__NOSTR_RELAYS__
