# Generated by gittr_ynh — do not edit by hand, see ynh_add_config calls in
# scripts/install, scripts/upgrade, scripts/change_url, scripts/config.
#
# Server-only vars (read via process.env at request time, NOT baked into
# the client bundle like conf/ui.env.j2's NEXT_PUBLIC_* ones) — loaded via
# systemd-ui.service's EnvironmentFile=, not inline Environment=, because
# this file can contain secrets and systemd unit files are world-readable.
# chmod 600, owned by __APP__ — see scripts/install.
GITHUB_CLIENT_ID=__GITHUB_CLIENT_ID__
GITHUB_CLIENT_SECRET=__GITHUB_CLIENT_SECRET__
GITHUB_REDIRECT_URI=https://__DOMAIN__/api/github/callback
GITHUB_PLATFORM_TOKEN=__GITHUB_PLATFORM_TOKEN__
PUBLISHER_BLOCKLIST=__PUBLISHER_BLOCKLIST__
